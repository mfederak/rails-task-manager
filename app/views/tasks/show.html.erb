<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<p> <%= link_to("Edit task", edit_task_path(@task.id)) %> </p>

<p> <%= link_to("Delete task", task_path(@task.id), method: :delete, data: { confirm: "Are you sure?" }) %> </p>
