<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <input type= "checkbox">
  <%= link_to(task.title, task_path(task.id)) %></br>
  <% end %>
</ul>

<p> <%= link_to("New task", new_task_path) %> </p>
